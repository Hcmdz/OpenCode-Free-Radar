<?xml version="1.0" encoding="utf-8"?>
<!-- All catalog traffic is HTTPS-only; user CAs trusted in debug builds only
     so release traffic cannot be proxied with a user-installed certificate. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
